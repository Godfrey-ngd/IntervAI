export const ROUTES = {
  auth: '/auth',
  authLogin: '/auth?mode=login',
  authRegister: '/auth?mode=register',
  resumeUpload: '/upload',
  knowledgebaseUpload: '/knowledgebase/upload',
} as const;

